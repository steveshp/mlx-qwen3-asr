# Memory Events: 2026-02

Append-only implementation events. Do not rewrite old entries for content
changes; append a new event instead.

## MEM-2026-02-16-001

- Date: 2026-02-16
- Scope: Introduced implementation-memory guidance and canonical memory log.
- Decision:
  - Use a flexible default format with a small minimum bar to preserve writing
    velocity while improving retrieval quality for future agents.
- What worked:
  - Explicitly requiring `Reuse next time` improved actionability.
  - Keeping guidance in `CLAUDE.md` reduced ambiguity for coding agents.
- What did not work:
  - Open-ended notes without structure were too inconsistent for reuse.
- Reuse next time:
  - Keep memory guidance simple: minimum bar plus optional richer fields.
- Evidence:
  - Commit references in `git log` touching memory policy docs.

## MEM-2026-02-16-002

- Date: 2026-02-16
- Scope: Migrated memory system to explicit two-track model:
  append-only `events/` plus compacted `operating-memory.md`.
- Decision:
  - Separate immutable events from compacted retrieval to improve traceability
    and reduce context-window load for active coding agents.
- What worked:
  - Adding `docs/memory/README.md` created a single update protocol.
  - Keeping `docs/IMPLEMENTATION_MEMORY.md` as a pointer preserved compatibility.
- What did not work:
  - Single-file memory/history format caused mixed concerns and weak compaction.
- Reuse next time:
  - For memory architecture changes, ship protocol + events + operating-memory
    + CLAUDE updates together.
- Evidence:
  - Files added under `docs/memory/`
  - `CLAUDE.md` updates
  - `.github/PULL_REQUEST_TEMPLATE.md`

## MEM-2026-02-16-003

- Date: 2026-02-16
- Scope: Consolidated memory protocol into `operating-memory.md` as a single
  front door and tightened wording to "non-trivial and meaningful work".
- Decision:
  - Keep one primary memory entry point to reduce navigation cost for agents
    while preserving append-only events as immutable history.
- What worked:
  - Moving protocol into operating memory removed overlap between docs.
  - Keeping `README.md` as a thin pointer preserved discoverability without
    duplication.
- What did not work:
  - Separate full protocol and operating docs created unnecessary doc-hopping.
- Reuse next time:
  - For agent UX, prefer one actionable entry doc plus append-only history.
- Evidence:
  - `docs/memory/operating-memory.md` protocol consolidation
  - `docs/memory/README.md` simplified index
  - `CLAUDE.md` and PR template wording updates

## MEM-2026-02-16-004

- Date: 2026-02-16
- Scope: Added `AGENTS.md` compatibility entrypoint while keeping `CLAUDE.md`
  as canonical instructions.
- Decision:
  - Use one canonical guide plus a thin compatibility delegator to support
    multiple agent ecosystems without policy duplication.
- What worked:
  - Minimal AGENTS shim improved discoverability for AGENTS-first runtimes.
- What did not work:
  - Missing AGENTS file left behavior ambiguous for agent tooling expecting
    that convention.
- Reuse next time:
  - When adding cross-ecosystem support, keep one source-of-truth and one thin
    compatibility file.
- Evidence:
  - `AGENTS.md`

## MEM-2026-02-16-005

- Date: 2026-02-16
- Scope: Added a multi-file streaming quality lane and hooked it into quality
  gate flow as an opt-in manifest-based check.
- Decision:
  - Close the highest-ROI streaming evaluation gap with one executable lane
    (`eval_streaming_manifest.py`) plus optional gate wiring, instead of adding
    new framework/scaffolding.
- What worked:
  - Reusing existing manifest conventions and gate helper patterns kept the
    implementation small and testable.
  - Explicit aggregate thresholds (`stability/rewrite/finalization`) made the
    lane deterministic for agent verification loops.
- What did not work:
  - Making it default strict-release would over-couple strict mode to local
    dataset availability, so it remains opt-in.
- Reuse next time:
  - For new eval lanes, add script + gate hook + tests + docs together in one
    slice, and keep heavy dataset lanes opt-in unless fixture-backed.
- Evidence:
  - `scripts/eval_streaming_manifest.py`
  - `scripts/quality_gate.py`
  - `tests/test_eval_streaming_manifest.py`
  - `tests/test_quality_gate.py`

## MEM-2026-02-16-006

- Date: 2026-02-16
- Scope: Hardened the streaming-manifest lane slice by fixing an accidental
  parity-test corruption and re-running full verification.
- Decision:
  - Keep the encoder optimization guardrail as a clean reference-loop parity
    test; remove accidental no-op noise so the test fails only on real
    regressions.
- What worked:
  - Full-suite + fast quality gate run after the fix confirmed no hidden
    regressions while keeping the change minimal.
- What did not work:
  - Large generated/no-op blocks in tests can silently mask intent and create
    false debugging paths.
- Reuse next time:
  - If a parity helper drifts, rewrite it directly against the production code
    path and run broad verification immediately.
- Evidence:
  - `tests/test_model.py`
  - `scripts/quality_gate.py`

## MEM-2026-02-16-007

- Date: 2026-02-16
- Scope: Added provenance metadata to streaming-manifest artifacts and
  regenerated committed smoke artifacts to match the updated schema.
- Decision:
  - Include lightweight reproducibility fields directly in evaluator output
    (`schema_version`, `generated_at_utc`, `git_commit`, `manifest_sha256`)
    rather than adding a separate artifact wrapper layer.
- What worked:
  - Keeping provenance generation inside `eval_streaming_manifest.py` made
    quality-gate and direct-script outputs consistent by construction.
- What did not work:
  - Initial test hash expectation was wrong; deterministic helper tests caught
    it quickly.
- Reuse next time:
  - Add helper-level tests whenever new artifact metadata is introduced, then
    regenerate checked-in artifacts in the same slice.
- Evidence:
  - `scripts/eval_streaming_manifest.py`
  - `tests/test_eval_streaming_manifest.py`
  - `docs/benchmarks/2026-02-16-streaming-manifest-smoke2.json`
  - `docs/benchmarks/2026-02-16-streaming-manifest-smoke2-gated.json`
