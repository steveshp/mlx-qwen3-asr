## MEM-2026-03-08-001

- Decision: Add a lightweight browser microphone web app directly to the existing FastAPI ASR server instead of introducing a separate frontend stack.
- Reuse next time: When the repo already has a single-process inference server, prefer static asset serving plus a thin browser client for operator-facing tools.
- Evidence: `server.py` already exposes `/health` and `/transcribe`, the ASR model is loaded process-wide via `Session`, and the user explicitly requested a web app for microphone transcription.
